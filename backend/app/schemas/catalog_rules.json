{
  "schema_version": "1.0",
  "name": "catalog_rules",
  "description": "SKU / GTIN / price validation rules applied by validate_skus to each canonical product.",
  "_TODO_step_4": [
    "Fill in the rule values below. validate_skus (Step 7) reads them via load_catalog_rules().",
    "sku.pattern: a regex for acceptable SKU strings.",
    "gtin.length (13) and require_valid_check_digit (true).",
    "price.min / price.max / allowed_currencies[].",
    "required_fields[].",
    "The `retail-solution` branch holds the reference values."
  ],
  "sku": {"pattern": null, "description": "TODO: SKU format regex."},
  "gtin": {"length": 13, "require_valid_check_digit": true},
  "price": {"min": null, "max": null, "allowed_currencies": []},
  "required_fields": []
}
